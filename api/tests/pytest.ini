[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    slow: marks tests as slow (LLM calls)
    unit: pure unit tests, no external dependencies
    integration: requires running API and Milvus
